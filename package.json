{
  "name": "transferencia-de-instancia",
  "private": true,
  "type": "module",
  "description": "Tela de transferência de atendimento entre números do Helena, com deploy na Vercel.",
  "scripts": {
    "dev": "node --env-file-if-exists=.env dev-server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
